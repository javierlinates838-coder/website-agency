import { VerifiedDesk } from "@/components/VerifiedDesk";
import { SiteFooter, SiteHeader } from "@/components/SiteHeader";

export default function DeskPage() {
  return (
    <div>
      <SiteHeader />
      <VerifiedDesk />
      <SiteFooter />
    </div>
  );
}
